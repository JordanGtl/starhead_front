import type { Metadata } from 'next';
import Components from '@/views/Components';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Composants', description: 'Boucliers, générateurs, refroidisseurs, quantum drives et tous les composants Star Citizen sur Star-Head.' };
export default Components;
