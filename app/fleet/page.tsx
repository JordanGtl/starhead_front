import Fleet from '@/views/Fleet';

export const metadata = {
  title: 'Fleet Visualizer — StarHead',
  description: 'Comparez vos vaisseaux Star Citizen à leur échelle réelle.',
};

export default function FleetPage() {
  return <Fleet />;
}
