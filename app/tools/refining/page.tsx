import type { Metadata } from 'next';
import RefiningSimulator from '@/views/tools/RefiningSimulator';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Simulateur de raffinage' };
export default RefiningSimulator;
