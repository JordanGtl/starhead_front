import type { Metadata } from 'next';
import Consumables from '@/views/Consumables';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Consommables' };
export default Consumables;
