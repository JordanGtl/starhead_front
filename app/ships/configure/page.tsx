import type { Metadata } from 'next';
import ShipConfigurator from '@/views/ShipConfigurator';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Configurateur de vaisseau — StarHead' };
export default ShipConfigurator;
