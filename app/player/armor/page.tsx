import type { Metadata } from 'next';
import PlayerArmor from '@/views/PlayerArmor';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Armures' };
export default PlayerArmor;
