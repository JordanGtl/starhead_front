import type { Metadata } from 'next';
import PlayerWeapons from '@/views/PlayerWeapons';

export const dynamic = 'force-dynamic';
export const metadata: Metadata = { title: 'Armes personnelles' };
export default PlayerWeapons;
